import React, { useState, useEffect } from 'react';

const HomeDepr = () => {

  useEffect(() => {
    window.location.href = "/preorder"
  });

  return (
    <div className="relative inset-0 flex flex-1 w-full h-full overflow-hidden">
      
    </div>
  );
};

export default HomeDepr;
